---
const invites = [
    { year: 2025, link: '/2025/' },
    { year: 2024 },
    { year: 2023 },
    { year: 2022 },
    { year: 2021 },
    { year: 2019 },
    { year: 2017 },
    { year: 2016 },
];
---
<section class="gallery-section">
    <div class="gallery-header">
        <span class="gallery-rule"></span>
        <h2 class="section-title">The Archive</h2>
        <span class="gallery-rule"></span>
    </div>

    <div class="timeline">
        <nav class="timeline-nav" aria-label="Year navigation">
            <div class="timeline-track">
                <a href="#countdown-hero" class="timeline-dot-link active" data-year="2026">
                    <span class="dot"></span>
                    <span class="dot-year">2026</span>
                </a>
                {invites.map((invite) => (
                    <a href={`#year-${invite.year}`} class="timeline-dot-link" data-year={invite.year}>
                        <span class="dot"></span>
                        <span class="dot-year">{invite.year}</span>
                    </a>
                ))}
            </div>
        </nav>

        <div class="timeline-content">
            {invites.map((invite) => (
                <div class="timeline-item" id={`year-${invite.year}`}>
                    <div class="timeline-item-year">
                        <span class="year-number">{invite.year}</span>
                    </div>
                    <div class="timeline-item-card">
                        <div class="card-image">
                            {invite.link ? (
                                <a href={invite.link} class="card-link">
                                    <img src={`/invites/hipster-holiday-${invite.year}.jpg`} alt={`Hipster Holiday ${invite.year}`} loading="lazy" />
                                    <span class="view-site">View Site &rarr;</span>
                                </a>
                            ) : (
                                <img src={`/invites/hipster-holiday-${invite.year}.jpg`} alt={`Hipster Holiday ${invite.year}`} loading="lazy" />
                            )}
                        </div>
                    </div>
                </div>
            ))}
        </div>
    </div>
</section>

<script>
    const hero = document.getElementById('countdown-hero');
    const items = document.querySelectorAll('.timeline-item');
    const dotLinks = document.querySelectorAll('.timeline-dot-link');

    function updateActiveDot() {
        const scrollCenter = window.innerHeight * 0.45;
        let activeId = '';

        if (hero) {
            const rect = hero.getBoundingClientRect();
            if (rect.top < scrollCenter && rect.bottom > scrollCenter - 100) {
                activeId = 'countdown-hero';
            }
        }

        items.forEach((item) => {
            const rect = item.getBoundingClientRect();
            if (rect.top < scrollCenter && rect.bottom > scrollCenter - 100) {
                activeId = item.id;
            }
        });

        dotLinks.forEach((link) => {
            const year = link.getAttribute('data-year');
            const linkId = year === '2026' ? 'countdown-hero' : `year-${year}`;
            link.classList.toggle('active', linkId === activeId);
        });
    }

    window.addEventListener('scroll', updateActiveDot, { passive: true });
    updateActiveDot();

    dotLinks.forEach((link) => {
        link.addEventListener('click', (e) => {
            e.preventDefault();
            const year = link.getAttribute('data-year');
            const targetId = year === '2026' ? 'countdown-hero' : `year-${year}`;
            const target = document.getElementById(targetId);
            if (target) {
                target.scrollIntoView({ behavior: 'smooth', block: year === '2026' ? 'start' : 'center' });
            }
        });
    });
</script>

<style>
    .gallery-section {
        max-width: 1100px;
        width: 100%;
        margin: 0 auto;
        padding: 64px 32px 120px;
        position: relative;
        z-index: 1;
    }

    .gallery-header {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 24px;
        margin-bottom: 72px;
    }

    .gallery-rule {
        display: block;
        flex: 1;
        max-width: 120px;
        height: 1px;
        background: linear-gradient(90deg, transparent, var(--gold), transparent);
        opacity: 0.5;
    }

    .section-title {
        font-family: var(--font-display);
        font-weight: 400;
        font-style: italic;
        font-size: 32px;
        color: var(--text-primary);
        text-align: center;
        letter-spacing: 0.02em;
    }

    .timeline {
        position: relative;
    }

    .timeline-nav {
        position: fixed;
        right: 24px;
        top: 50%;
        transform: translateY(-50%);
        height: fit-content;
        z-index: 10;
    }

    .timeline-track {
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        padding: 16px 0;
    }

    .timeline-track::before {
        display: none;
    }

    .timeline-dot-link:not(:last-child) .dot::after {
        content: '';
        position: absolute;
        top: 100%;
        left: 50%;
        width: 1px;
        height: 28px;
        background: rgba(255, 255, 255, 0.06);
        transform: translateX(-50%);
    }

    .timeline-dot-link {
        position: relative;
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 14px 0;
        text-decoration: none;
        cursor: pointer;
        z-index: 1;
    }

    .dot {
        position: relative;
        display: block;
        width: 7px;
        height: 7px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.12);
        border: 1.5px solid rgba(255, 255, 255, 0.08);
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .dot-year {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 12px;
        letter-spacing: 1px;
        color: var(--text-dim);
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        white-space: nowrap;
    }

    .timeline-dot-link:hover .dot {
        background: rgba(255, 255, 255, 0.3);
        transform: scale(1.3);
    }

    .timeline-dot-link:hover .dot-year {
        color: var(--text-mid);
    }

    .timeline-dot-link.active .dot {
        background: var(--accent);
        border-color: var(--accent);
        transform: scale(1.5);
        box-shadow: 0 0 12px rgba(196, 52, 45, 0.5);
    }

    .timeline-dot-link.active .dot-year {
        color: var(--text-primary);
        font-weight: 500;
    }

    .timeline-content {
        display: flex;
        flex-direction: column;
        gap: 120px;
        max-width: 720px;
        margin: 0 auto;
    }

    .timeline-item {
        display: flex;
        align-items: flex-start;
        gap: 32px;
        scroll-margin-top: 25vh;
    }

    .timeline-item-year {
        flex-shrink: 0;
        width: 0;
        padding-top: 20px;
        margin-left: -48px;
        margin-right: 16px;
    }

    .year-number {
        font-family: var(--font-display);
        font-size: 20px;
        color: var(--text-dim);
        writing-mode: vertical-lr;
        text-orientation: mixed;
        letter-spacing: 2px;
        transform: rotate(180deg);
    }

    .timeline-item-card {
        flex: 1;
        min-width: 0;
    }

    .card-image {
        position: relative;
        border-radius: 12px;
        overflow: hidden;
        background: var(--bg-card);
        box-shadow: 0 8px 40px rgba(0, 0, 0, 0.4);
    }

    .card-image img {
        width: 100%;
        display: block;
        transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .card-image:hover img {
        transform: scale(1.03);
    }

    .card-link {
        display: block;
        position: relative;
    }

    .view-site {
        position: absolute;
        bottom: 16px;
        right: 16px;
        font-family: var(--font-body);
        font-size: 12px;
        font-weight: 500;
        letter-spacing: 2px;
        text-transform: uppercase;
        color: var(--text-primary);
        background: rgba(6, 8, 16, 0.7);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        padding: 8px 16px;
        border-radius: 100px;
        opacity: 0;
        transform: translateY(4px);
        transition: opacity 0.3s, transform 0.3s;
    }

    .card-link:hover .view-site {
        opacity: 1;
        transform: translateY(0);
    }

    @media (max-width: 768px) {
        .gallery-section {
            padding: 48px 20px 80px;
        }

        .gallery-header {
            margin-bottom: 40px;
        }

        .section-title {
            font-size: 26px;
        }

        .timeline {
            flex-direction: column;
        }

        .timeline-nav {
            position: static;
            transform: none;
            right: auto;
            width: 100%;
            margin-bottom: 32px;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }

        .timeline-track {
            flex-direction: row;
            justify-content: center;
            flex-wrap: wrap;
            padding: 0;
        }

        .timeline-track::before {
            display: none;
        }

        .timeline-dot-link {
            flex-direction: column;
            padding: 8px 12px;
            gap: 6px;
        }

        .dot-year {
            font-size: 11px;
        }

        .timeline-content {
            max-width: none;
            gap: 72px;
        }

        .timeline-item {
            flex-direction: column;
            gap: 12px;
        }

        .timeline-item-year {
            width: auto;
            padding-top: 0;
        }

        .year-number {
            writing-mode: horizontal-tb;
            transform: none;
            font-size: 18px;
            color: var(--text-mid);
        }

        .card-image {
            border-radius: 10px;
        }

        .view-site {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
